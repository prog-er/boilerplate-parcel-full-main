include ../globals.pug
include ../layout/content-wrap
include ../layout/header

block variables

<!DOCTYPE html>
html(lang=lang)
  head
    each value in meta
      meta&attributes(value)

    title=pageTitle + ' - ' + projectName

    each link in links
      link&attributes(link)

    each script in scripts
      script&attributes(script)

  body
    block header
      +header

    block main